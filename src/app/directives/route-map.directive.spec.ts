import { RouteMapDirective } from './route-map.directive';

describe('RouteMapDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteMapDirective();
    expect(directive).toBeTruthy();
  });
});
